<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Redirect Domains
    |--------------------------------------------------------------------------
    |
    | Куда разрешено возвращать пользователя после экрана согласия. Клиента
    | коннекторы заводят сами (RFC 7591, POST /oauth/register), поэтому список
    | здесь — единственное, что мешает постороннему зарегистрировать клиента
    | с чужим redirect_uri и увести код авторизации.
    |
    | Сверяется префикс «схема + хост». Появится ещё один коннектор — добавить
    | его домен строкой; отказ виден в ответе /oauth/register как
    | invalid_redirect_uri. "*" разрешает вообще всё и здесь не нужен.
    |
    */

    'redirect_domains' => [
        'https://claude.ai',            // Claude, веб и десктоп
        'https://claude.com',
        'https://chatgpt.com',          // ChatGPT
        'https://chat.openai.com',
        'https://platform.openai.com',
        'https://grok.com',             // Grok
        'https://x.ai',
        'http://localhost',             // MCP Inspector и mcp-remote
        'http://127.0.0.1',
    ],

    /*
    |--------------------------------------------------------------------------
    | Allowed Custom Schemes
    |--------------------------------------------------------------------------
    |
    | Настольные клиенты возвращаются не по http, а по своей схеме (RFC 8252).
    |
    */

    'custom_schemes' => [
        'claude',
        'cursor',
        'vscode',
    ],

    /*
    |--------------------------------------------------------------------------
    | Authorization Server
    |--------------------------------------------------------------------------
    |
    | Issuer в метаданных RFC 8414 и RFC 9728. null — берётся APP_URL,
    | что здесь и нужно: authorization server и сам ресурс на одном домене.
    |
    */

    'authorization_server' => null,

];
